{
  "name": "root",
  "private": true,
  "workspaces": ["packages/*"],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.9",
    "typescript": "^5",
    "@types/node": "^20"
  },
  "scripts": {
    "devel:build": "npx lerna run build --stream"
  }
}
