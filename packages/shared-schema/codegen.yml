schema: './schema/**/*.graphql'
generates:
  generated/types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: true
      federation: true
      enumsAsTypes: true

